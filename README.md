# A Project with PySide2

## How to run

You must use Python>=3.8

Install the python package requirements:

```bash
python -m pip install -r requirements.txt
```

And run:
```bash
python main.py
```
